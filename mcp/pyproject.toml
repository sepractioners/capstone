[project]
name = "clm-mcp-server"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "contract-lifecycle",
    "mcp>=2.0",
]

[tool.setuptools.packages.find]
include = ["clm_mcp_server*"]
