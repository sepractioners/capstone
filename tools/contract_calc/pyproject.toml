[project]
name = "contract-calc"
version = "0.1.0"
description = "Deterministic date and money calculation helpers for contracts"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["contract_calc*"]
