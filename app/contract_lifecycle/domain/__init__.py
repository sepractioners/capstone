"""Domain layer: value objects, entities, the Contract aggregate, domain
services, domain events, and repository interfaces.

This layer has no dependency on persistence, UI, or external systems.
"""
